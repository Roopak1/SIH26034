# Leave this as /api for local development with Vite's proxy.
# Set this to the deployed backend URL plus /api in Vercel, for example:
# VITE_API_BASE_URL=https://your-backend-service.onrender.com/api
VITE_API_BASE_URL=/api
